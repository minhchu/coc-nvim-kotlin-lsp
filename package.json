{
  "name": "coc-kotlin-lsp",
  "version": "0.0.2",
  "description": "coc.nvim extension for Kotlin Language Server (kotlin-lsp).",
  "license": "MIT",
  "main": "./lib/index.js",
  "engines": {
    "coc": ">=0.0.78",
    "node": ">=18"
  },
  "activationEvents": [
    "onLanguage:kotlin"
  ],
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Kotlin LSP",
      "properties": {
        "kotlin-lsp.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable Kotlin LSP language client."
        },
        "kotlin-lsp.command": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Custom command to launch kotlin-lsp. When null, use bundled binary downloaded at npm postinstall."
        },
        "kotlin-lsp.args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "--stdio"
          ],
          "description": "Arguments passed to the kotlin-lsp command."
        },
        "kotlin-lsp.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace communication between coc.nvim and kotlin-lsp."
        },
        "kotlin-lsp.java.check": {
          "type": "boolean",
          "default": true,
          "description": "Warn when Java 17+ is not available."
        },
        "kotlin-lsp.autoUpdate": {
          "type": "boolean",
          "default": true,
          "description": "Automatically check for and install new kotlin-lsp releases on startup (at most once per day). Only applies when using the bundled server."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "prepare": "npm run build",
    "postinstall": "node scripts/download-kotlin-lsp.mjs",
    "upgrade": "node scripts/download-kotlin-lsp.mjs"
  },
  "dependencies": {
    "coc.nvim": "^0.0.83-next.9"
  },
  "devDependencies": {
    "@types/node": "^18.19.130",
    "typescript": "^5.8.3"
  }
}
